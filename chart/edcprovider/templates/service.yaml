apiVersion: v1
kind: Service
metadata:
  name: {{ include "edcprovider.fullname" . }}
  labels:
    {{- include "edcprovider.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "edcprovider.selectorLabels" . | nindent 4 }}
